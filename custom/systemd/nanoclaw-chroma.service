[Unit]
Description=NanoClaw ChromaDB Vector Store
After=network.target

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/home/john/workspace/nanoclaw
ExecStart=/usr/bin/docker compose -f docker-compose.chroma.yml up -d
ExecStop=/usr/bin/docker compose -f docker-compose.chroma.yml down
Environment=HOME=/home/john
Environment=PATH=/usr/local/bin:/usr/bin:/bin:/home/john/.local/bin

[Install]
WantedBy=default.target
